<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- PERMISOS DE RED -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- PERMISOS DE NOTIFICACIÓN -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- PERMISOS DE SENSORES -->
    <uses-permission android:name="android.permission.BODY_SENSORS" />
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />
    <uses-permission android:name="android.permission.HIGH_SAMPLING_RATE_SENSORS" />

    <!-- PERMISOS DE FOREGROUND SERVICE -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <!-- PERMISOS DE WEAR OS -->
    <uses-permission android:name="com.google.android.gms.permission.WEARABLE_BINDENER" />

    <application
        android:name="com.ace.mobile.MobileApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Acemobile"
        android:usesCleartextTraffic="false">

        <activity
            android:name="com.ace.mobile.presentation.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Acemobile">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.ace.mobile.presentation.diagnostic.WearDiagnosticActivity"
            android:exported="true"
            android:label="A.C.E Wear Diagnostic" />

        <!-- FOREGROUND SERVICE: dataSync -->
        <service
            android:name="com.ace.mobile.service.ExerciseSyncService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- WEAR OS LISTENER: escucha datos y mensajes del reloj -->
        <service
            android:name="com.ace.mobile.service.WearDataListenerService"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="com.google.android.gms.wearable.DATA_CHANGED" />
                <data
                    android:host="*"
                    android:pathPrefix="/ace/"
                    android:scheme="wear" />
            </intent-filter>
            <intent-filter>
                <action android:name="com.google.android.gms.wearable.MESSAGE_RECEIVED" />
                <data
                    android:host="*"
                    android:pathPrefix="/ace/"
                    android:scheme="wear" />
            </intent-filter>
        </service>

        <!-- FIX BUG 1: Deshabilitar WorkManagerInitializer por defecto para usar Configuration.Provider manual -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                tools:node="remove" />
        </provider>

    </application>

</manifest>